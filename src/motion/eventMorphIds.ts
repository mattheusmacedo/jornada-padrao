// Shared layoutIds for the RAYE event card → detail-overlay morph (matches
// motion-primitives MorphingDialog). Both the trigger card on Perfil and the
// EventMorphOverlay reference these constants so the names stay in sync.
export const RAYE_MORPH_IDS = {
  container: 'event-raye-1-container',
  image: 'event-raye-1-image',
  title: 'event-raye-1-title',
} as const
